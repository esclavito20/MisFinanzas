name: Prueba del secreto de firma

on:
  workflow_dispatch:

jobs:
  prueba:
    name: Comprobar el secreto
    runs-on: ubuntu-latest
    steps:
      - name: Sin secreto
        run: echo "El flujo arranca sin referenciar secretos"
